import * as React from 'react'
import { Link, Heading, Container, ChakraProvider } from '@chakra-ui/react'
import PageWrapper from './PageWrapper'

export default () => (
  <PageWrapper>
    <Container>
      WIP
    </Container>
  </PageWrapper>
)